# URL of the SmartClinic backend (Socket.io server).
# Local default is http://localhost:3001. In production set this to the
# deployed backend URL on Render/Railway.
VITE_SERVER_URL=http://localhost:3001
