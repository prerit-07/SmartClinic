# Port the server listens on (Render/Railway inject PORT automatically)
PORT=3001

# Comma-separated list of allowed origins for CORS / Socket.io.
# Use the deployed frontend URL(s). "*" allows any origin (fine for MVP/demo).
CORS_ORIGIN=*
